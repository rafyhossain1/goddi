# Netlify deploy config for goddi.sbyc.cloud
# Static site — no build step.

[build]
  publish = "."

# Security headers — sane defaults for a static game with no auth.
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options       = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy       = "strict-origin-when-cross-origin"
    Permissions-Policy    = "interest-cohort=()"
    # Allow self-scripts + Google Fonts + Cloudflare Web Analytics beacon +
    # Cloudflare Turnstile (script + iframe widget); data: needed for inline
    # SVG favicon
    Content-Security-Policy = "default-src 'self'; script-src 'self' https://static.cloudflareinsights.com https://challenges.cloudflare.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data:; media-src 'self'; connect-src 'self' https://kafvftjcvnvtklqwanrm.supabase.co https://cloudflareinsights.com https://challenges.cloudflare.com; frame-src https://challenges.cloudflare.com; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'self';"

# Aggressive cache for static assets, keyed by Netlify's hash so cache busts
# automatically on each deploy.
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/css/*"
  [headers.values]
    # No cache while we're still tuning the game post-launch. Browser
    # revalidates on each request (cheap — Netlify returns 304 if unchanged).
    # Revisit this in a few weeks once content stabilises; bump to 86400.
    Cache-Control = "no-cache, must-revalidate"

[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "no-cache, must-revalidate"

[[headers]]
  for = "/data/*"
  [headers.values]
    Cache-Control = "public, max-age=300, must-revalidate"

# index.html itself should never aggressively cache, so new deploys appear
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Make /stats resolve to /stats.html so the admin dashboard has a clean URL
[[redirects]]
  from = "/stats"
  to   = "/stats.html"
  status = 200

# /rules → /rules.html — public lottery rules page
[[redirects]]
  from = "/rules"
  to   = "/rules.html"
  status = 200

# /about → /about.html — about page for the game
[[redirects]]
  from = "/about"
  to   = "/about.html"
  status = 200
