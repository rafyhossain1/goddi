# OS
.DS_Store
Thumbs.db
*.swp
*~

# Editor
.vscode/
.idea/

# Node (in case any tooling gets added later)
node_modules/
npm-debug.log*

# Local
.env
.env.local
*.local
# Local-only SQL that carries real secrets — never commit these
*.local.sql
